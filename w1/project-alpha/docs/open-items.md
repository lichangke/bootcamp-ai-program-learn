# Open Items (Post Day 8)

- Date: 2026-02-18
- Status: no blocking open items

## 1. Blocking Issues

None.

## 2. Non-Blocking Follow-Ups (Optional Next Iteration)

1. Add explicit `system` theme mode (`light` / `dark` / `system`) in UI.
2. Add additional E2E cases against a real backend (non-mocked API path).
3. Add API docs page generation (OpenAPI static export for sharing).
