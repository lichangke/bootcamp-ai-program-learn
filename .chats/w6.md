# instrutions

## simple agent 构建

基于 @specs/w6/0001-simple-agent-design.md 的规范，构建一个 agent sdk，提供 agent 的核心功能，用户可以很方便地为 agent 添加自定义工具和 mcp。 完成构建后，确保所有实现符合 design spec，并提供几个 example 来展示如何使用（包含至少一个使用 mcp 的例子）。代码在 w6/simple-agent， Think hard。